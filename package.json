{
  "name": "spotify-premium-playlists",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "workers:dev": "yarn build && wrangler dev",
    "workers:publish": "yarn build && wrangler publish"
  },
  "dependencies": {
    "@headlessui/vue": "^0.2.0",
    "vue": "^3.0.4"
  },
  "devDependencies": {
    "@vue/compiler-sfc": "^3.0.4",
    "autoprefixer": "^9",
    "postcss": "^7",
    "tailwindcss": "^2.0.1@compat",
    "vite": "1.0.0-rc.13"
  }
}
